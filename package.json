{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "telease": "turbo run build --filter=@elysium/docs && changelog publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.9",
    "turbo": "^1.5.5"
  },
  "dependencies": {
    "phosphor-react": "^1.4.1"
  }
}
